import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF7C5CFC);
  static const userBubble = Color(0xFFE8E0FF);
  static const aiBubble = Color(0xFFF5F3FF);
}

class AppConstants {
  static const appName = 'Memora';
  static const serverBaseUrl = 'https://memora-api.example.com';
  static const soulGenerationRounds = [5, 15, 30];
}
